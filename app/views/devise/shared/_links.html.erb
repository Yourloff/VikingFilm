<div class="mt-4">
  <% if user_signed_in? %>
    <%= link_to 'Выйти из системы', destroy_user_session_path, method: :delete %>
  <% else %>
    <%= link_to 'Зарегистрироваться', new_user_registration_path, class: 'border p-2 hover:bg-sky-500/75 hover:shadow-md hover:text-gray-50' %>
    <%= link_to 'Войти', new_user_session_path, class: 'border p-2 hover:bg-sky-500/75 hover:shadow-md hover:text-gray-50' %>
  <% end %>
</div>

